class MatterState {
  static Solid = new MatterState('solid');
  static Liquid = new MatterState('liquid');
  static Gas = new MatterState('gas');

  constructor(name) { this.name = name; }
}

class Organ {
  
}

